'use client';

import { motion } from 'framer-motion';
import type { WorkflowStage } from '@/app/page';

interface OutlinePanelProps {
  // placeholder - this component is now merged into BriefPanel
}

export default function OutlinePanel() {
  return null;
}
